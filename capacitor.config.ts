import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prova_SilvioLucas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
